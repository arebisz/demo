steps:
- name: hashicorp/terraform:latest
  id: init 
  args: ['init']
- name: hashicorp/terraform:latest
  args: ['apply', '-auto-approve']
  waitFor:
    - init

logsBucket: 'gs://$PROJECT_IDterraform-patching-tfstate'
options:
  logStreamingOption: STREAM_ON
  logging: GCS_ONLY

tags: ['cos']
