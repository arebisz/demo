steps: 
- name: hashicorp/terraform:latest
  id: init 
  args: ['init', '-reconfigure']

- name: hashicorp/terraform:latest
  args: ['apply', '-auto-approve']
  waitFor:
    - init
    
logsBucket: 'gs://cos_terraform_state'
options:
  logStreamingOption: STREAM_ON
  logging: GCS_ONLY
